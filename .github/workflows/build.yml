name: Build MAUI App (Windows Only For Now)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release 版本号（例：v1.0.0）'
        required: true

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: DavinciPreset/src/PresetEditor
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore dependencies
        run: dotnet restore

      - name: Publish Windows App
        run: dotnet publish PresetEditor.csproj -c Release -f net9.0-windows10.0.19041.0

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: |
            **/bin/Release/net9.0-windows10.0.19041.0/win-x64/publish/**

  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Setup .NET SDK
  #       uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: 9.0.x

  #     - name: Install MAUI workload
  #       run: dotnet workload install maui

  #     - name: Restore dependencies
  #       run: dotnet restore

  #     # 可以在这里加签名参数 CodesignKey 等
  #     - name: Publish MacCatalyst App
  #       run: dotnet publish PresetEditor.csproj -c Release -f net9.0-maccatalyst

  #     - name: Upload MacOS Artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: macos-release
  #         path: |
  #           **/bin/Release/net9.0-maccatalyst/maccatalyst-arm64/publish/**
  
  release:
    runs-on: ubuntu-latest
    needs: [build-windows]
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: windows-release
          path: windows-release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            windows-release/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

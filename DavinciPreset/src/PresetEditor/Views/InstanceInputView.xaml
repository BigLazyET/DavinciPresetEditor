<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:PresetEditor.ViewModels"
             xmlns:models="clr-namespace:PresetEditor.Models"
             x:Class="PresetEditor.Views.InstanceInputView"
             x:DataType="viewModels:InstanceInputViewModel">
    <VerticalStackLayout>
        <Label Text="{Binding InstanceInput.InputName}" />
        <CollectionView ItemsSource="{Binding InstanceInput.PropertyList}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:InstanceInputItem">
                    <HorizontalStackLayout>
                        <Label Text="{Binding Key}"
                               FontAttributes="Italic" />
                        <Label Text=": " />
                        <Entry Text="{Binding Value}"
                               Margin="10,0,0,0" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentView>
